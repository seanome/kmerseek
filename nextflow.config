// Nextflow configuration for kmerseek pipeline

// Get current date for file naming
def dateFormat = new java.text.SimpleDateFormat('yyyy-MM-dd')
def today = dateFormat.format(new java.util.Date())

// Enable execution reports
trace {
    enabled = true
    file = "kmerseek_scope40.${today}.trace.txt"
    fields = 'task_id,hash,native_id,process,tag,name,status,exit,module,container,cpus,time,disk,memory,attempt,submit,start,complete,duration,realtime,queue,%cpu,%mem,rss,vmem,peak_rss,peak_vmem,rchar,wchar,syscr,syscw,read_bytes,write_bytes'
}

timeline {
    enabled = true
    file = "kmerseek_scope40.${today}.timeline.html"
}

report {
    enabled = true
    file = "kmerseek_scope40.${today}.report.html"
}

// Process configuration
process {
    // Default resource allocation
    cpus = 1
    memory = '4 GB'
    time = '12h'

    // Process-specific resources
    withName: buildRelease {
        cpus = 2
        memory = '8 GB'
        time = '30m'
    }

    withName: indexDatabase {
        cpus = 2
        memory = '100 GB'
        time = '24h'
        maxForks = 1  // Each index uses 27-54 GB RAM, must run one at a time
    }

    withName: searchAllVsAll {
        cpus = 2
        memory = '100 GB'
        time = '24h'
        maxForks = 1  // Run 1 search at a time (TEA k=10 needs ~92GB, use 100GB to be safe)
    }

    withName: searchShuffledVsOriginal {
        cpus = 2
        memory = '100 GB'
        time = '24h'
        maxForks = 1
    }

    // Human-mouse ortholog pipeline processes
    withName: downloadOrthologMapping {
        cpus = 1
        memory = '1 GB'
        time = '10m'
    }

    withName: parseOrthologMapping {
        cpus = 1
        memory = '4 GB'
        time = '30m'
    }

    withName: decompressFasta {
        cpus = 1
        memory = '2 GB'
        time = '30m'
    }

    withName: searchHumanVsMouse {
        cpus = 2
        memory = '100 GB'
        time = '24h'
        maxForks = 1
    }

    withName: evaluateOrthologs {
        cpus = 1
        memory = '8 GB'
        time = '1h'
    }

    withName: aggregateResults {
        cpus = 1
        memory = '1 GB'
        time = '10m'
    }
}

// Executor configuration
executor {
    name = 'local'
    cpus = 8
    memory = '110 GB'  // Leave ~18 GB for OS and other processes
}
